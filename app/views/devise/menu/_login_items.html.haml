- if user_signed_in?
  %li= link_to "#{current_user.first_name} #{current_user.last_name}", edit_profile_path
  %li= link_to('Sair', destroy_user_session_path)
- else
  %li= link_to('Login', new_user_session_path)